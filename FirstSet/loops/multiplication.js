var mult = 9;
for(var i=0; i<=10; i++){
  var answer = mult * i;

  console.log("The answer of " + mult + " * " + i + " is " + answer);
}

for (var multiplier = 0; multiplier <= 10; multiplier++) {
 for (var i = 0; i <= 10; i++) {
   var answer = multiplier * i;
   console.log(multiplier + " * " + i + " = " + answer);
 }
}